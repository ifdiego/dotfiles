# Dotfiles

This is my Bash configuration.  
For now, it's really simple, it only has `bash`, `git` and `vscode` files.

## Install

To install just run: 
```
./setup.sh
```

Maybe, before install it, you need to give `setup.sh` permission with the command:
```
chmod +x setup.sh
```